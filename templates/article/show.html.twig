{% extends "base.html.twig" %}

{% block main %}
{{ parent() }}
    <div>
        Hello, welcome to {{ article }}.
    </div>
    <div>For a list of items:
        <ul>
            {% for item in items %}
                <li>
                    {{ item }}
                </li>
            {% endfor %}
        </ul>
    </div>

{% endblock %}
