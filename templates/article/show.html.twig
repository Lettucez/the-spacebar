{% extends "base.html.twig" %}

{% block main %}
{{ parent() }}
    <div>
        Hello, welcome to {{ title }}.
    </div>
    <div>For a list of items:
        <ul>
            {% for item in items %}
                <li>
                    {{ item }}
                </li>
            {% endfor %}
        </ul>
    </div>

{% endblock %}
