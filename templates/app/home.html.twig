{% extends 'base.html.twig' %}

{% block main %}
    <div class="box">
        <div class="content">
            <h2>This is the home page.</h2>
            <p>Not much stuff going on tbh fam</p>
            
            <p>Article 1: <a href="{{ path('article_show', { slug: 'what-killed-the-dinosaurs'}) }}">View</a></p>
        </div>
    </div>

{% endblock %}